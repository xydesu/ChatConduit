# ChatConduit 伺服器手動測試檢查清單 (In-Game Server Test Checklist)

本檢查清單專為伺服器管理員與測試人員設計，用於在真實 Minecraft 伺服器環境中逐步驗證 `ChatConduit` 的所有功能、GUI 選單、權限與整合模組。

---

## 📋 測試前準備 (Prerequisites)

1. 將 `ChatConduit-1.0.jar` 放入伺服器的 `plugins/` 目錄並啟動伺服器。
2. 準備至少 **2 位測試玩家** (建議一位具備 OP/管理員權限，另一位為普通玩家權限)。
3. (選用) 準備 Discord Webhook 網址與 Redis 測試環境。

---

## 🧪 手動測試步驟 (Manual Test Cases)

### 1. 💬 基礎頻道與發言測試 (Basic Chat & Channels)
- [ ] **1.1 開啟 GUI 選單**：在遊戲內輸入 `/channel` 或 `/ch`，確認開啟箱子選單 GUI。
- [ ] **1.2 官方頻道切換**：在 GUI 中點擊「交易頻道」或「全域頻道」，確認聊天欄顯示切換成功提示。
- [ ] **1.3 快捷前綴發言**：在預設全域頻道下，輸入 `$這是一筆交易`，確認訊息直接發送至交易頻道，前綴顯示為 `[交易]`。
- [ ] **1.4 頻道懸停與點擊互動 (Hover & Click)**：
  - 將滑鼠游標懸停在聊天欄的頻道 Prefix（如 `[全域]` 或 `[交易]`）上，確認彈出懸停視窗顯示簡介、守則與類型。
  - 左鍵點擊聊天欄的頻道 Prefix，確認自動執行切換頻道指令。
- [ ] **1.5 顏色碼權限測試**：
  - 給予玩家 `chatconduit.chat.color` 權限，輸入 `&a綠色文字 &c紅色文字`，確認顯示彩色。
  - 移除該權限後再次發言，確認顏色碼被視為普通純文字 `&a綠色文字`。

---

### 2. 👥 玩家自建群組頻道測試 (Custom Player Channels)
- [ ] **2.1 對話框建立頻道**：
  - 輸入 `/pc` 開啟群組頻道 GUI，點擊「建立新頻道」。
  - 聊天欄出現對話框提示，輸入 ` cancel `，確認成功取消建立作業。
  - 重新點擊建立頻道，輸入 `測試戰隊`，確認建立成功並自動切換至該頻道。
- [ ] **2.2 保留字與名稱長度限制測試**：
  - 嘗試建立名稱為 `admin` 或 `global` 的頻道，確認系統提示「包含系統保留字」。
  - 嘗試建立超過 20 個字元的頻道名稱，確認系統提示長度無效。
- [ ] **2.3 邀請玩家與雙行互動按鈕**：
  - 隊長輸入 `/pc invite <玩家B>`，玩家 B 聊天欄收到 2 行訊息。
  - 玩家 B 點擊 `[✖ 點擊拒絕]`，確認隊長收到拒絕通知。
  - 隊長再次邀請，玩家 B 點擊 `[✔ 點擊接受]`，確認玩家 B 成功加入頻道。
- [ ] **2.4 權限模式切換 (PUBLIC / PRIVATE)**：
  - 隊長在 GUI 設定中將模式改為 `PUBLIC (公共)`。
  - 未受邀的玩家 C 點擊該頻道，確認無需邀請即可直接加入。
- [ ] **2.5 頻道細節編輯**：
  - 隊長在 GUI 修改「頻道顯示名稱」、「頻道簡介」、「頻道守則」。
  - 點擊「色彩主題」，確認頻道前綴漸層色彩隨點擊切換。
- [ ] **2.6 隊長管理功能**：
  - 測試踢出成員：在 GUI 玩家頭像上按左鍵，確認將該成員踢出。
  - 測試轉讓隊長：在 GUI 成員頭像上按右鍵，確認隊長職位成功移交。
  - 測試解散頻道：點擊「解散頻道」，確認所有成員自動切換回預設頻道，且選單清單中該頻道消失。

---

### 3. 🤖 Discord Webhook 綁定與測試 (Discord Webhook Integration)
- [ ] **3.1 綁定 Webhook URL**：
  - 在群組頻道 GUI 點擊「綁定 Webhook」，貼上 Discord Webhook URL。
- [ ] **3.2 測試 Webhook 連線按鈕**：
  - 點擊 GUI 中的「測試 Webhook 連線」按鈕。
  - 確認遊戲內收到 `✅ Webhook 連線測試成功！` 提示與音效，且 Discord 頻道收到測試卡片訊息。
- [ ] **3.3 Webhook 訊息實時同步**：
  - 成員在群組頻道發言，確認 Discord 頻道同步出現帶有該玩家名稱與頭像的對話。
- [ ] **3.4 解除綁定**：在對話框輸入 `clear`，確認成功解除綁定。

---

### 4. ⚡ Redis 多伺服器跨服廣播測試 (Redis Multi-Server Test - 需雙服)
- [ ] **4.1 跨服訊息同步**：伺服器 A 玩家在全域頻道發言，伺服器 B 玩家能實時收到，並帶有 `[伺服器A]` 來源標籤。
- [ ] **4.2 跨服邀請同步**：伺服器 A 隊長邀請伺服器 B 玩家，伺服器 B 玩家收到通知並點擊接受，兩服成員名單實時同步。

---

### 5. 🗄️ 效能與資料保留測試 (Performance & Data Persistence)
- [ ] **5.1 登入/離線資料記憶**：選擇特定頻道後登出伺服器重新登入，確認自動恢復上次選擇的頻道，且登入時無任何畫面凍結或 Lag。
- [ ] **5.2 熱重載測試**：輸入 `/chatconduit reload`，確認提示 reload 成功，且所有頻道設定正常運作。
